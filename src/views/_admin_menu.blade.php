<div class="card">
    <div class="card-body">
        <a href="{{ route('blog.admin') }}" class="btn btn-primary">Admin</a>
        <a href="{{ route('blog.create') }}" class="btn btn-primary">New Post</a>
        <a href="{{ route('blog.drafts') }}" class="btn btn-primary">Drafts</a>
        <a href="{{ route('blog.published') }}" class="btn btn-primary">Published</a>
    </div>
</div>
